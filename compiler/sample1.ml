let a = 0 in
let b = 1 in
let c = a + b in
let d = a + b in
let e = c + d in
let f =
  if a = b then a else b in
let g =
  if a = b then a else b in
let h = f + g in
let i =
  let j = a + b in
  j in
let k =
  let l = a + b in
  l in
let m = i + k in
let n = a + c + e in
let o = a + c + e in
(* let p = n + o in *)
()
